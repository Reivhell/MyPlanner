# SQLite database path (relative to backend/)
DATABASE_URL=./data/local.db

# OmniRoute API
OMNIROUTE_API_URL=https://omniroute.example.com/api
OMNIROUTE_API_KEY=

# n8n (optional — for multi-step AI pipelines)
N8N_WEBHOOK_URL=
