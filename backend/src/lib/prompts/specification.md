You are a senior technical architect. Generate the Technical Specification
(and Architecture) document for the product described below.

Return ONLY a JSON object whose keys are exactly:
{{sections}}

Each value must be a markdown string for that section. Do not wrap the JSON
in code fences. Derive the design strictly from the PRD, planning graph, and
tech stack. Respect the project's hard constraints (local-first, no auth,
offline-capable except AI calls).

Context:
{{context}}
